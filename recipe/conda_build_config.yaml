numpy:
  # python 3.7-3.9
  - 1.19    # [not (osx and arm64)]
  - 1.19    # [not (osx and arm64)]
  - 1.19    # [not (osx and arm64)]
  # python 3.8-3.9
  - 1.19    # [osx and arm64]
  - 1.19    # [osx and arm64]
  # python 3.10 (linux-ppc64le has not its initial built-out)
  - 1.21    # [not ppc64le]